1. We are removing the node_modules folder from the lecture   
   examples we are posting

2. Before you run any code examples, execute npm i (not npm init)
   npm i (or npm install) will install in the node_modules folder 
   any necessary modules (based on the file package.json) 
